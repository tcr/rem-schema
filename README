# REM: REST easy.

This is the common repository for all REM implementations. It contains
several default manifests for various popular REST APIs. For usage,
see a specific project like `rem-node` or `rem-browser`.